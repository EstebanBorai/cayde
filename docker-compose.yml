version: '3'

services:
  database:
    build: ./.docker
    ports:
      - "5432:5432"
    volumes:
       - db-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: "cupboard_api"
      POSTGRES_PASSWORD: "cupboard_api"
      POSTGRES_DB: "cupboard_api"

volumes:
  db-data: